## Ergonomic alias for GUI macro APIs.
## Import this module instead of `cps/gui/macro` to avoid keyword collisions.

from "macro" import guiFile, guiInline, guiBlock, guiBuild, guiBridge

export guiFile, guiInline, guiBlock, guiBuild, guiBridge
